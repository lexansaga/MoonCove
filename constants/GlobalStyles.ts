import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
  overlink: {
    top: 0,
    left: 0,
    height: "100%",
    width: "100%",
    position: "absolute",
  },
});
